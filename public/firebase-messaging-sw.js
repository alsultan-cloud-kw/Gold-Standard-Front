/* Auto-generated by vite — do not edit by hand. */
/* eslint-disable no-restricted-globals */
importScripts('https://www.gstatic.com/firebasejs/11.10.0/firebase-app-compat.js')
importScripts('https://www.gstatic.com/firebasejs/11.10.0/firebase-messaging-compat.js')

firebase.initializeApp({
  apiKey: "AIzaSyDXmSVhx9pMdSFy6JxG5wM_UI1e4J3AxNw",
  authDomain: "goldstandard-dcca4.firebaseapp.com",
  projectId: "goldstandard-dcca4",
  storageBucket: "goldstandard-dcca4.firebasestorage.app",
  messagingSenderId: "277505358501",
  appId: "1:277505358501:web:96b54751f8a302e69dab4c",
})

const messaging = firebase.messaging()

messaging.onBackgroundMessage((payload) => {
  const title =
    (payload.notification && payload.notification.title) ||
    (payload.data && payload.data.title) ||
    'Gold Standard'
  const body =
    (payload.notification && payload.notification.body) ||
    (payload.data && payload.data.body) ||
    ''
  const url =
    (payload.data && (payload.data.url || payload.data.deep_link)) || '/'
  const icon =
    (payload.notification && payload.notification.icon) ||
    '/favicons/android-chrome-192x192.png'

  return self.registration.showNotification(title, {
    body,
    icon,
    badge: '/favicons/favicon-32x32.png',
    data: { url, ...(payload.data || {}) },
  })
})

self.addEventListener('notificationclick', (event) => {
  event.notification.close()
  const target = (event.notification.data && event.notification.data.url) || '/'
  const absolute = String(target).startsWith('http')
    ? target
    : new URL(target, self.location.origin).href

  event.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clientList) => {
      for (const client of clientList) {
        if ('focus' in client) {
          client.navigate?.(absolute)
          return client.focus()
        }
      }
      if (self.clients.openWindow) {
        return self.clients.openWindow(absolute)
      }
      return undefined
    }),
  )
})
